const sectionList = {
  about: "about",
  skills: "skills",
  projects: "projects",
  contact: "contact",
} as const;

const sectionPaths = {
  about: "#about",
  skills: "#skills",
  projects: "#projects",
  contact: "#contact",
} as const;

export { sectionList, sectionPaths };
